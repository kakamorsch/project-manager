class Activity {
  constructor(id, projectId, name, startDate, endDate, completed) {
    this.id = id;
    this.projectId = projectId;
    this.name = name;
    this.startDate = startDate;
    this.endDate = endDate;
    this.completed = completed;
  }
}

export default Activity;
